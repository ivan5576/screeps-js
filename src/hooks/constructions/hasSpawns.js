// export const hasConstructionSites = (gameRoomObj) => {
//   const constructionSites = gameRoomObj.find(FIND_MY_CONSTRUCTION_SITES);
//   if (Array.isArray(constructionSites) && (constructionSites.length > 0)) { return constructionSites; }
//   else if (Array.isArray(gameRoomObj) && (gameRoomObj.length === 0)) { return false; }
//   else return null;
// };
